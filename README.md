echo "# Remote Staffing Backend" > README.md
git add README.md .github/workflows/build-layer.yml
git commit -m "Initial commit: Add GitHub Actions workflow to build Snowflake lambda layer"
git push origin main
